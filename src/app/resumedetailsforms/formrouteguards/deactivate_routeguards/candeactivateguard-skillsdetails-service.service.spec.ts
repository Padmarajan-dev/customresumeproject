import { TestBed } from '@angular/core/testing';

import { CandeactivateguardSkillsdetailsServiceService } from './candeactivateguard-skillsdetails-service.service';

describe('CandeactivateguardSkillsdetailsServiceService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: CandeactivateguardSkillsdetailsServiceService = TestBed.get(CandeactivateguardSkillsdetailsServiceService);
    expect(service).toBeTruthy();
  });
});
