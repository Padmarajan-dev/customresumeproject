import { TestBed } from '@angular/core/testing';

import { CandeactivateguardResumetopdetailsService } from './candeactivateguard-resumetopdetails.service';

describe('CandeactivateguardResumetopdetailsService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: CandeactivateguardResumetopdetailsService = TestBed.get(CandeactivateguardResumetopdetailsService);
    expect(service).toBeTruthy();
  });
});
