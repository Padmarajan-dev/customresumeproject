import { TestBed } from '@angular/core/testing';

import { CandeactivateguardPortfoliodetailsServiceService } from './candeactivateguard-portfoliodetails-service.service';

describe('CandeactivateguardPortfoliodetailsServiceService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: CandeactivateguardPortfoliodetailsServiceService = TestBed.get(CandeactivateguardPortfoliodetailsServiceService);
    expect(service).toBeTruthy();
  });
});
