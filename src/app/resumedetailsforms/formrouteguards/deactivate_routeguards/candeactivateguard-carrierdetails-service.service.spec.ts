import { TestBed } from '@angular/core/testing';

import { CandeactivateguardCarrierdetailsServiceService } from './candeactivateguard-carrierdetails-service.service';

describe('CandeactivateguardCarrierdetailsServiceService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: CandeactivateguardCarrierdetailsServiceService = TestBed.get(CandeactivateguardCarrierdetailsServiceService);
    expect(service).toBeTruthy();
  });
});
