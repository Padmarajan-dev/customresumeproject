import { TestBed } from '@angular/core/testing';

import { CandeactivateguardAboutusdetailsServiceService } from './candeactivateguard-aboutusdetails-service.service';

describe('CandeactivateguardAboutusdetailsServiceService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: CandeactivateguardAboutusdetailsServiceService = TestBed.get(CandeactivateguardAboutusdetailsServiceService);
    expect(service).toBeTruthy();
  });
});
