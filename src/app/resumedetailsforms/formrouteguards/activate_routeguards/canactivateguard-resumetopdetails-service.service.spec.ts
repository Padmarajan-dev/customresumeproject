import { TestBed } from '@angular/core/testing';

import { CanactivateguardResumetopdetailsServiceService } from './canactivateguard-resumetopdetails-service.service';

describe('CanactivateguardResumetopdetailsServiceService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: CanactivateguardResumetopdetailsServiceService = TestBed.get(CanactivateguardResumetopdetailsServiceService);
    expect(service).toBeTruthy();
  });
});
